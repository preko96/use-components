import styled from 'styled-components'

export default styled.div(standard)

const standard = {
	display: 'flex',
	flexBasis: 'auto',
	flexGrow: 0,
	flexShrink: 0,
	alignItems: 'center',
	justifyContent: 'flex-start',
}
